const productModel = require('./product.model');
const salesModel = require('./sales.model');
const insertIdSale = require('./sales.model');

module.exports = {
  productModel,
  insertIdSale,
  salesModel,
};